[plugins.prometheus-exporter]
host = "0.0.0.0"
prefix = ""
suffix = "_alumet"
port = 9091
add_attributes_to_labels = true

[plugins.rapl]
poll_interval = "1s"
flush_interval = "5s"
no_perf_events = false
